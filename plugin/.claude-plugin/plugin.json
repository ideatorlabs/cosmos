{
  "name": "cosmos",
  "version": "0.1.0",
  "description": "Connects the session to the cosmos of every shared folder that has .cosmos/: Charter, Ledger, flares, Atlas, handoffs.",
  "mcpServers": {
    "cosmos": {
      "command": "python3",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/bin/cosmos-mcp"
      ]
    }
  },
  "author": {
    "name": "cosmos"
  }
}
